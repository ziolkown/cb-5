// Testprogramm aus Testsuite

float test(int x, int y) {
	int a = 1;
	int b = 2;
	float c = 3.5;
	float d = 4.5;
	bool e = true;
	bool f = false;

	c = a + true * c / (d * a) - (a/d);

	return a + b;
}

void main() {
	int a = 1;
	int b = 2;
	printf(test(a,b));
}
